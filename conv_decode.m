function output = conv_decode(input, A)
%Use Viterbi to decode the input sequence, return decoded sequence
%Inputs: input = inputted bit sequence
%        A = convolution encoding projection matrix (See conv_encode)

end